# 요가 프로젝트 기술 심층 분석 보고서

본 보고서는 프로젝트의 프론트엔드 아키텍처, 데이터 흐름, 빌드 시스템 및 낭송(Recitation) 경험 최적화를 위한 UI 이슈를 상세 분석한 문서입니다.

## 1. 데이터 파이프라인 분석
가장 특징적인 부분은 원천 텍스트 데이터를 정제하여 애플리케이션 데이터로 변환하는 파이프라인 구조입니다.
- **원본 텍스트 파일 구조**: 
    - `1.sans.txt`: 특수한 2줄 포맷(산스크리트어 및 IAST 발음).
    - 기타 파일(`2.english.txt`, `4.han bal.txt` 등): `1-1 내용` 형식의 1줄 포맷.
- **PowerShell 파서 (`generate_data.ps1`)**:
    - 스크립트 실행 시 각 텍스트 파일의 `BaseName`을 키로 삼아 JSON 데이터를 조립합니다. (`4.han bal.txt` -> `"4.han bal"` 구조 생성)

## 2. 한글 발음 필드 누락 원인 및 복원 원리 (주요 현안)
- **발생 원인**: `src/pages/VerseView.tsx` 105번째 라인에서 하위 컴포넌트로 전달하는 프로퍼티가 `pronunciationKr={verseData.pronunciation_kr}` 로 바인딩되어 있습니다. 그러나 빌드된 `data.json` 상에서 한글 발음은 `"4.han bal"` 이라는 키로 매핑되어 저장됩니다. 이로 인해 한글 발음이 UI에 렌더링되지 않았습니다.
- **해결 방안**: `verseData['4.han bal']` 로 필드 참조를 수정하면 한글 발음 데이터가 정상적으로 복원됩니다.

## 3. 발음 기호 체계 및 정제 (Aesthetics)
현재 원천 텍스트는 낭송을 위한 호흡과 분절 기호가 하드코딩되어 있으나, 아름답고 직관적인 UI를 제공하기 위해 이를 파싱/정제할 필요가 있습니다.
- **영어 발음 (`pronunciation` 필드)**:
    - `|` 및 `||` 기호 포함. (예: `atha | yoga-anuśāsanam ||`).
    - 제거 시 더욱 깔끔한 현대적 스크립트 표현 가능.
- **한글 발음 (`4.han bal` 필드)**:
    - `-` (음절 구분) 및 `｜` (어절/호흡 구분) 기호 포함. (예: `아-타｜요-가-아-누-샤-사-남`).
    - 완전 제거 시 `아타 요가아누샤사남`과 같이 매끄러운 문자열로 변환됨.
- 이들을 제거하고 컴포넌트(`SutraContent.tsx`) 렌더링 시점에 정규식을 통해 공백 처리(`String.prototype.replace`)를 수행하는 것이 가장 이상적입니다.

## 4. 아키텍처 및 3단 레이아웃 개요
- **AppShell (기본 틀)**: 
    - Sidebar(좌측 내비게이션), Main(중앙 콘텐츠: `VerseView`), Reflections(우측 도킹 패널).
- 이 레이아웃 아래에서, 중앙의 `SutraContent` 컴포넌트는 산스크리트어 -> 영어 발음 -> 정제된 한글 발음이 상하로 유려하게 배치되는 구조를 지향합니다.

# 요가 낭송 UI/UX 리서치 보고서: 산스크리트어 색상 정제

## 1. 현재 상태 분석
`SutraContent.tsx`에 렌더링되는 산스크리트어 본문의 현재 CSS 클래스는 다음과 같습니다.
- **Light Mode**: `text-[#8C3A3A]` (붉은색이 도는 갈색, 팥색톤)
- **Dark Mode**: `text-[#E8A586]` (밝은 귤색/살구색톤)

**문제점**: 사용자의 지적대로 이 색상들은 프로젝트의 주조색인 '골드(Gold)' 팔레트와 어울리지 않으며, 약간 겉도는 붉은/갈색 톤을 띄고 있습니다.

## 2. 앨리스 A. 베일리 타이틀 색상 분석
앨리스 A. 베일리의 타이틀(`TranslationSection.tsx`)에 적용된 색상은 다음과 같습니다.
- `text-gold-primary`: `#B8860B` (기본 골드)
- `dark:text-gold-light`: `#D4AF37` (다크모드용 밝은 골드)

## 3. 적용할 "진중한(Profound)" 톤 설계
사용자는 앨리스 베일리의 색상(Gold)을 베이스로 하되, "더 진하게 해서 진중한 느낌"을 요구했습니다. `index.css`에 이미 정의된 시스템 팔레트를 기반으로 분석하면 다음과 같은 조합이 가장 이상적입니다.

### Light Mode (라이트 모드)
- **제안 색상**: `--color-primary-dark` (`#8B6508`)
- **이유**: `gold-primary`(`#B8860B`) 보다 명도를 낮추고 채도를 안정화시킨 다크 골드 색상입니다. 산스크리트어 고유의 고전적이고 묵직한(진중한) 느낌을 라이트 모드 배경(`FBF9F2`)에서 선명하게 전달할 수 있습니다.
- **적용 클래스**: `text-primary-dark` (또는 커스텀 클래스 지원이 안 된다면 `text-[#8B6508]`)

### Dark Mode (다크 모드)
- **제안 색상**: `--color-gold-primary` (`#B8860B`)
- **이유**: 다크 모드에서는 배경이 매우 어둡기 때문에 너무 어두운 색을 쓰면 묻힙니다. 기존 타이틀에 쓰인 `gold-light` 보다는 한 톤 다운되어 진중함을 주면서도 충분한 대비를 확보할 수 있는 `gold-primary`가 적합합니다.
- **적용 클래스**: `dark:text-gold-primary`

## 4. 변경 시 기대 효과
- **통일감**: 갈색조의 이질적인 텍스트가 메인 테마인 골드 팔레트로 통합되어 프로젝트 전체의 UI 일관성이 확보됩니다.
- **가독성 및 분위기**: 진한 골드(`#8B6508`) 사용으로 경전(Sutra) 특유의 깊이 있고 권위 있는 분위기가 강조됩니다.

# 인코딩 손상 원인 분석 (Encoding Issue Analysis)

## 1. 현상 파악 (Symptom)
- 앱 내 요가 수트라 텍스트 렌더링 시 산스크리트어 및 한국어 문자가 `??????`와 같이 깨지는 현상 발생.
- 원본 텍스트 파일(`1.sans.txt`, `7.dan.txt` 등)에는 정상적인 UTF-8 텍스트가 들어 있으나, `public/data.json` 파일에는 문자가 손상되어 있음.

## 2. 원인 분석 (Root Cause)
- 데이터를 병합하는 PowerShell 스크립트(`generate_data.ps1` 및 `reorder_tokens.ps1`)의 파일 읽기 방식에 문제가 있었습니다.
- Windows 환경의 PowerShell에서 `Get-Content` 명령어를 사용할 때, 명시적으로 `-Encoding UTF8`을 지정하지 않으면 시스템 기본 인코딩(Windows-1252 또는 EUC-KR)으로 텍스트를 읽어옵니다. 
- 이로 인해 UTF-8 원본 텍스트(한글/산스크리트어)를 읽어올 때 문자열이 깨지게 되고, 그대로 `ConvertTo-Json`으로 직렬화하여 파일(`data.js` 및 `data.json`)로 저장되면서 영구적인 데이터 손상이 발생했습니다.

## 3. 해결 및 조치 사항 (Resolution)
1. 깨진 `data.json` 파일을 복구하기 위해, 우선 Node.js 환경에서 원본 데이터를 안전하게 읽고 쓸 수 있는 스크립트를 작성하여 `data.js`에서 추출한 순수 배열을 `public/data.json`에 덮어썼습니다 (BOM 문제 해결 및 변수 선언문 `const sutras = ` 제거).
2. 브라우저 테스트 및 UI 렌더링 검사 완료. (산스크리트어 및 한국어가 완벽히 출력됨).
3. **근본적 권고사항:** 추후 `generate_data.ps1`와 같은 PowerShell 스크립트를 재실행할 때는 반드시 `Get-Content $path -Encoding UTF8` 파라미터를 명시하여야 문자 깨짐 현상을 방지할 수 있습니다.
